# exemptions for tests/enforcement/test_no_bespoke_reuse.py (gu-task-06, GU-06-07).
#
# format (one entry per violation, each preceded by exactly one rationale line):
#   # rationale: <specific reason, >= 30 chars, not a blanket phrase>
#   relative/posix/path.py:LINE:Symbol
#
# LINE may be a concrete line number or '*' for a file-wide exemption of that symbol.
#
# the sanctioned reuse targets (the traced-httpx wrapper modules and the
# ConversationWriteBuffer periodic-flush primitive) are ALLOWLISTED in the walker
# itself, not here -- they are the components bespoke code is meant to reuse, not
# exceptions to the rule.

# rationale: WriteBuffer is internal plumbing USED BY BaseCollection itself -- flush_pending() drains its entries and calls persist_to_postgres() on the registered Collection. Converting it to a BaseCollection subclass would be circular (a Collection using a Collection to buffer writes to a Collection). Its SQLiteBackend usage is the primitive's own deferred-write journal, not a bespoke wrapper. Same exemption the cache guard carries.
packages/core/src/threetears/core/collections/flush.py:91:WriteBuffer

# rationale: ToolServer's 'self._tools' is the pod's TOOL REGISTRY -- a name->TearsTool lookup written by register_tool() and read by the dispatch path -- not an accumulator of pending writes; nothing in the class ever drains or flushes it. The while+sleep the fingerprint pairs it with is the NATS heartbeat/re-registration supervisor (publish a HeartbeatMessage, re-publish registration, crash the pod on a sustained-unhealthy streak), which persists nothing. ConversationWriteBuffer is a write-behind buffer over a Collection and has no meaning for either half, so 'reuse it' names no reachable change. Flagged only because the src-root widening brought packages/agent/* into scan range for the first time.
packages/agent/tools/src/threetears/agent/tools/server.py:*:ToolServer

# rationale: LocalHttpServer is a scripted test HTTP server, not a write path. Its 'self.requests' list RECORDS the request heads it served so a test can assert on them, and its asyncio.sleep is a scripted per-reply 'delay' for pinning deadline behaviour -- there is nothing buffered and nothing flushed, so ConversationWriteBuffer has no meaning here. It lives under src/ (not tests/) because a consumer host must be able to pin what its own injected transport does with a socket, which is why the walker sees it at all.
packages/search/src/threetears/search/testing/http_server.py:*:LocalHttpServer
