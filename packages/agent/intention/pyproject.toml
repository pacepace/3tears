[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "3tears-agent-intention"
version = "0.15.0"
description = "Standing-wants corpus for LLM agents -- intention lifecycle, salience decay, embedding dedup, and private deliberation tools"
readme = "README.md"
requires-python = ">=3.14"
authors = [{name = "pace"}]
license = "MIT"
license-files = ["LICENSE"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Framework :: AsyncIO",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries",
    "Typing :: Typed",
]
# In-repo (3tears*) deps are declared only when a module imports them:
# the dependency-alignment enforcement gate flags a declared 3tears*
# dep that nothing imports. B1 (schema + collection) imports core +
# observe only; 3tears-agent-acl (owner RBAC) and 3tears-langgraph
# (FrameworkEvents) land with the code that imports them in B2. The
# external forward-declarations below are the package's B2 tool surface.
dependencies = [
    "3tears",
    "3tears-observe",
    "pgvector",
    "langchain-core",
    "pydantic",
    "uuid-utils",
]

[project.urls]
Repository = "https://github.com/pacepace/3tears"

[tool.hatch.build.targets.wheel]
packages = ["src/threetears"]

[tool.uv.sources]
3tears = { workspace = true }
3tears-observe = { workspace = true }
